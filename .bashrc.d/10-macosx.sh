#!/bin/bash
# silence zsh bagger message on mac
if [[ "$(uname)" == "Darwin" ]]; then
  export BASH_SILENCE_DEPRECATION_WARNING=1
fi

# silence zsh bagger message on mac
if [[ "$(uname)" == "Darwin" ]]; then
  export BASH_SILENCE_DEPRECATION_WARNING=1
fi

